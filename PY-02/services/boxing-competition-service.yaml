apiVersion: v1
kind: Service
metadata:
  name: boxing-competition-service
  labels:
    app: boxing-competition
spec:
  selector:
    app: boxing-competition
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer
